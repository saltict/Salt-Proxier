# Salt-Proxier Configuration

# Server port (default: 3000)
PORT=3000

# Proxy configuration (optional)
# Format: username:password@host:port or host:port
# Example: PROXY=user:pass@proxy.example.com:8080
PROXY=

# CORS allowed origins (default: *)
# Example: CORS=https://myapp.com
CORS=*

# Bearer token for authentication (optional)
# Example: BEARER_TOKEN=my-secret-token-123
BEARER_TOKEN=

# Headers to forward to target server (comma-separated, optional)
# Example: ALLOW_HEADERS=authorization,content-type,x-api-key
ALLOW_HEADERS=

# Path prefixes to strip before forwarding (comma-separated, optional)
# Example: STRIP_PREFIXES=kalshi,poly
STRIP_PREFIXES=

# Rust log level (default: info)
# Options: error, warn, info, debug, trace
RUST_LOG=info
